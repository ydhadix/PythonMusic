# start()

Start the ramp from its current value toward its target.

```python
linearramp.start()
```

Calls your function right away with the starting value, then again at each step until it reaches the target. If the ramp was already running, it restarts from where it is.
