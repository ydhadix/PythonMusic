# setDelay()

Set how long the oscillator waits between updates.

```python
oscillatortimer.setDelay(delay)
```

## Parameters

```python
oscillatortimer.setDelay(delay)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `delay` | `int or float` | _required_ | The new wait between updates, in milliseconds. |
