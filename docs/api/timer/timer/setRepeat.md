# setRepeat()

Set whether the timer repeats.

```python
timer.setRepeat(repeat)
```

## Parameters

```python
timer.setRepeat(repeat)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `repeat` | `bool` | _required_ | True to keep calling the function at the interval, False to call it just once. |
