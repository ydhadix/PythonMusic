# setRate()

Set how often automation runs.

```python
Automate.setRate()
```

## Parameters

```python
Automate.setRate(frameRate=60)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `frameRate` | `int` | `60` | The new rate, in frames per second. |
