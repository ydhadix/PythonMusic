# getColor()

Return the object's [Color](../../color/index.md).

## Parameters

Once an object `item` has been created, you can use the following function:

```python
item.getColor()
```

## Returns

`return color`

| Value | Type | Description |
|---|---|---|
| color | `Color` | The current color. |
