# clearDrawing()

Erase everything drawn with the draw… methods.

```python
display.clearDrawing()
```

This clears the shapes drawn by [drawRectangle()](drawRectangle.md), [drawLine()](drawLine.md), and the other draw… methods. GUI objects added with [add()](add.md) are not affected.
