# setTitle()

Set the display's title.

## Parameters

Once an object `display` has been created, you can use the following function:

```python
display.setTitle(title)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `title` | `str` | _required_ | The new window title. |
