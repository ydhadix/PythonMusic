# show()

Show the display.

This happens automatically when a display is created, so you only need it after calling [hide()](hide.md).

## Parameters

Once an object `display` has been created, you can use the following function:

```python
display.show()
```
