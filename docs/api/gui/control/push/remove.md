# remove()

Remove an object from the group.

```python
push.remove(item)
```

The object keeps its current on-screen place as it leaves.

## Parameters

```python
push.remove(item)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `item` | `Drawable` | _required_ | The object to remove. |
