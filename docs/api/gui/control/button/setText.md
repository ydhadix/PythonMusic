# setText()

Set the button's text.

## Parameters

Once an object `button` has been created, you can use the following function:

```python
button.setText(text)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `text` | `str` | _required_ | The new text to show on the button. |
