# addSeparator()

Add a separator line to the menu.  Useful for grouping related items.

## Parameters

Once an object `menu` has been created, you can use the following function:

```python
menu.addSeparator()
```
