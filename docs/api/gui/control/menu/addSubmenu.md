# addSubmenu()

Add a submenu to the menu.

```python
menu.addSubmenu(menu)
```

Used to build nested, hierarchical menus.

## Parameters

```python
menu.addSubmenu(menu)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `menu` | `Menu` | _required_ | The menu to nest inside this one. |
