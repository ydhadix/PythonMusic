# setToolTipText()

Set the hover text shown over the object.

```python
vfader.setToolTipText()
```

## Parameters

```python
vfader.setToolTipText(text=None)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `text` | `str` | `None` | The tooltip text. If omitted, the tooltip is cleared. |
