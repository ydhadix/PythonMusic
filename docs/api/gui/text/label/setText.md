# setText()

Set the label's text.

```python
label.setText(text)
```

## Parameters

```python
label.setText(text)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `text` | `str` | _required_ | The new text. If it is longer than the label can fit, it is truncated. |
