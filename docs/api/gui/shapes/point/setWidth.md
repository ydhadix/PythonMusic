# setWidth()

Set the circle's diameter.

```python
point.setWidth(width)
```

Sets both the width and the height, keeping the circle round.

## Parameters

```python
point.setWidth(width)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `width` | `int or float` | _required_ | The new diameter, in pixels. |
