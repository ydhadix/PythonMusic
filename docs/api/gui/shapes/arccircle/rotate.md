# rotate()

Turn the object by an additional angle.

```python
arccircle.rotate(angle)
```

The angle is added to the object's current rotation.

## Parameters

```python
arccircle.rotate(angle)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `angle` | `int or float` | _required_ | How far to turn, in degrees, counter-clockwise. |
