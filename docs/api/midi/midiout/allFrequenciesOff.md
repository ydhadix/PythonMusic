# allFrequenciesOff()

Stop every frequency from sounding, on all channels.

```python
midiout.allFrequenciesOff()
```

Same as [allNotesOff()](allNotesOff.md), and also resets pitch bend on every channel.
