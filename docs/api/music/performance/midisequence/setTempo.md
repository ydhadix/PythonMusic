# setTempo()

Set the sequence's playback tempo.

```python
midisequence.setTempo(bpm)
```

## Parameters

```python
midisequence.setTempo(bpm)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `bpm` | `int or float` | _required_ | The new tempo, in beats per minute. |
