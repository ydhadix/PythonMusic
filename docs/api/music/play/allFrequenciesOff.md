# allFrequenciesOff()

Stop every frequency from sounding, on all channels.

```python
Play.allFrequenciesOff()
```

Same as [Play.allNotesOff()](allNotesOff.md), and also resets pitch bend on every channel.
